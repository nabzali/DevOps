pipeline {
    agent any
    stages {
      stage('Applying Git Tag') {
        steps {
            script {
              String tag = params['Tag']
              echo tag
              sh 'git branch'
            }
        }
      }
    }
}
