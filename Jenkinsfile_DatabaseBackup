pipeline {
    agent any
    stages {
      stage('Applying Database Backup') {
        steps {
            script {
              def dbName = params['dbName']
              def serverNameSuffix = ".tc.mazepoint.local"
              
              
            }
        }
      }
    }
}
