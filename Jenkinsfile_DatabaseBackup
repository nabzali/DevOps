pipeline {
    agent any
    stages {
      stage('Applying Database Backup') {
        steps {
            script {
              def dbName = params['dbName']
              serverNameSuffix = ".tc.mazepoint.local"
              
              
            }
        }
      }
    }
}
