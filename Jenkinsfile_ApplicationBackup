pipeline {
    agent any
    stages {
      stage('Applying Database Backup') {
        steps {
            script {
              def appName = params['appName']
              
            }
        }
      }
    }
}
